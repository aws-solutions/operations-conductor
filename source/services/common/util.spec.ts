import { CommonUtil } from './util';

const COMMON_UTIL = new CommonUtil();

// TODO: make sure to test every edge case
describe('CommonUtil', () => {
    describe('isStringValuesValid', () => {
        test('success', (done) => {
            done();
        });
    });

    describe('isObjectEmpty', () => {
        test('success', (done) => {
            done();
        });
    });

    describe('getErrorObject', () => {
        test('success', (done) => {
            done();
        });
    });

    describe('sendAnonymousMetric', () => {
        test('success', (done) => {
            done();
        });
    });
});